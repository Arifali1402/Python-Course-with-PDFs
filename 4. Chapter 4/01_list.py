# Create a List using []

a = [2,54,6,7,4]
# Print the list using print() function 
print(a)

#Access using index using a[0],a[1] etc

print(a[0])
#Change the value of an element in the list (because the elements of the list are mutable)
a[0] = 1
print(a)

# we can create a list with items of defferent types
c = [45,"Arif", False, 6.9]
print(c)